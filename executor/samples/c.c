#include <stdio.h>
int main() {
	//Program that prints "Hello world" to the console
	printf("Hello world");

	// Sample code for adding two numbers, taken from the stdin
	// int a, b;
	// printf("Please enter values for a and b: ");
	// scanf("%d", &a);
	// scanf("%d", &b);
	// printf("%d + %d = %d", a, b, a+b);
	return 0;
}
